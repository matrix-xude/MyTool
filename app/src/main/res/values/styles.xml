<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>


    <style name="PopBottom" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/pop_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/pop_bottom_out</item>
    </style>

    <style name="PopTop" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/pop_top_in</item>
        <item name="android:windowExitAnimation">@anim/pop_top_out</item>
    </style>


    <declare-styleable name="PinYinSideBar">
        <attr name="pinyin_letterSize" format="dimension" />
        <attr name="pinyin_letterColor" format="color" />
        <attr name="pinyin_focusLetterColor" format="color" />
        <attr name="pinyin_letterSpacingExtra" format="float" />
    </declare-styleable>

</resources>
